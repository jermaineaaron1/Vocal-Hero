# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.suo
*.user

# Logs / temp
*.log

# Dependencies
/node_modules/

# Next.js
/.next/
/out/
next-env.d.ts

# Production build
/build

# Vercel
/.vercel/

# TypeScript
*.tsbuildinfo

# Local env files (never commit secrets)
.env
.env*.local

# Python pipeline secrets — already covered by the broader .env rule
# above, called out explicitly since this folder has its own .env.
python-pipeline/.env
python-pipeline/.git

# Local-only backup of python-pipeline's old git history (contains a
# secret in an old commit) — never commit this.
/python-pipeline-old-git-history-backup/
